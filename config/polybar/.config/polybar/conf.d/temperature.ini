[module/temperature]
type = internal/temperature
thermal-zone = 0
warn-temperature = 70
hwmon-path = ${env:POLYBAR_CPU_HWMON}

format = <ramp> <label>
format-warn = <ramp> <label-warn>

label = %temperature-c%
label-warn = %temperature-c%
label-warn-foreground = ${colors.alert}

ramp-0 = 
ramp-foreground = ${colors.foreground}
